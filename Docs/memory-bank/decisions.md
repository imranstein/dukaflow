# Decisions

A lightweight decision log. Anything architectural graduates to a formal ADR in `docs/adr/` once code exists (see source of truth, section 4). This file is for the smaller calls that still shouldn't get relitigated.

| Date | Decision | Why |
|------|----------|-----|
| 2026-08-08 | `Docs/` (capital D) holds project management docs; `docs/` (lowercase, created in Phase 0) will hold code docs and ADRs | Keeps planning notes separate from the docs that ship with the codebase |
